## Run

```bash
npm install
npm run dev
```

This runs the Vite frontend on `http://localhost:3000/`.

For the full stack:

```bash
npm run dev:full
```

This starts the React frontend on `3000` and the Express backend in `initialApp/` on `4000`.
