language: node_js
node_js:
  - "18"
os: linux

install:
  - npm install
  - npm --prefix initialApp install

script:
  - npm test
  - npm --prefix initialApp test

after_script:
  - npx --prefix initialApp codecov -f initialApp/coverage/lcov.info || echo "Codecov upload failed"




