language: node_js
node_js:
  - "18"
os: windows

before_install:
  - cd initialApp

install:
  - npm install
  - npm install cross-env --no-save
  - npm install codecov --no-save

  script:
  - npm test
  - ls -R coverage
  - cat coverage/lcov.info
  - npx codecov -f coverage/lcov.info

env:
  global:
     - CODECOV_TOKEN=77cc6ece-fd65-426a-b228-aef3d6b70a31
 

